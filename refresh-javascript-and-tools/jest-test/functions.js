const functions = {
    add: (num1, num2) => num1 + num2,
    isNull: () => null,
    // isNull: () => undefined,
    checkValue: x => x,
    createUser: () => {
        const user = { firstName : "Akhil" }
    }

}


module.exports = functions;